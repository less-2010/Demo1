package day18.se03.n1Collection;

import java.util.ArrayList;
import java.util.List;

public class Demo03List {
    public static void main(String[] args) {
        /**
         * List接口继承自Collection接口，是单列集合的一一个重要分支，习惯上我们把.
         * 实现了List接口的集合称之为List集合。，
         * 1、允许出现重复的元素，所有元素以线性方式存储，在程序中可以通过索引来
         * 访问集合中指定的元素。
         * 2、元素有序，元素的存入顺序和取出顺序是一- 致的。
         */
        List<String> list= new ArrayList<>();

//        public void add(int index,E element) ;将指定元素添加到指定位置
        list. add("小明");
        list. add("小白");
        list. add("小明白");
        list.add(2, "小清楚");
        System. out . println(list);
//        public E get(int index) ;返回集合中指定位置的元素
        System. out . println(list.get(2));
//        public E remove(int index) ;移除集合中指定位置的元素，返回移除的
        System. out . println(list. remove(2));
        System. out. println(list);
//        public E set(int index,E element);用指定元素替换集合中
        System.out.println(list.set(1,"大清楚"));
        System.out.println(list);


























































































































































































































































































































































































































































































































































































































































































































    }
}
